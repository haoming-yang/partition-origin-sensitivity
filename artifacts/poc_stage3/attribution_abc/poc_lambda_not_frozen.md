# POC_LAMBDA_NOT_FROZEN

Status: C not run.

The current runnable Stage3 code and protocol directory do not contain a frozen POC consistency coefficient or a runnable POC implementation. A value of `lambda=1` appears only in an older archived paper package, which is not an executable, pinned Stage3 protocol. The Stage3 rule therefore forbids selecting an arbitrary coefficient. Condition B (two-view supervised loss only) may proceed; condition C is intentionally stopped at this gate.
